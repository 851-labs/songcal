pre-commit:
  parallel: true
  commands:
    format:
      run: bun run format
    lint:
      run: bun run lint
    types:
      run: bun run types

commit-msg:
  commands:
    commitlint:
      run: bunx commitlint --edit {1}
